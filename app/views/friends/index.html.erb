<p style="color: green"><%= notice %></p>


<body style="background-color:#A7C0CA;">
<table class="table table-striped table-bordered">
  <thead class="thead-dark">
    <tr>
      <th>Frist name</th>
      <th>Last name</th>
      <th>Email</th>
      <th>Phone no</th>
      <th>user id</th>

      <th colspan="3">Action</th>


  </tr>
    </thead>

<tbody>

<% @friends.each do |friend| %>
<% if friend.user == current_user %>
<tr>
  <td><%=  friend.first_name %></td>
  <td><%=  friend.last_name %></td>
  <td><%=  friend.email %></td>
  <td><%=  friend.phone_no %></td>
  <td><%=  friend.user_id %></td>
  <td><%= link_to 'view',friend %></td>
  <td><%= link_to 'Edit',edit_friend_path(friend) %></td>
  <td><%= button_to "Destroy this friend", friend, method: :delete %></td>



</tr>
 <%end%>
  <%end%>


  </tbody>
  </table>
  <%= paginate @friends %>
  </body>

<%=link_to "Export Friends", friends_path(format: 'csv') %>

<%= link_to "New friend", new_friend_path%>

<%= search_form_for @q, url: :friends do |form|%>
<div class="form-group">
  <%= form.label :search_by_first_name, style: "display: block" %>
  <%= form.text_field :first_name_cont %>
</div>
<div class="form-group">
<%= form.submit 'Search Friend',class: "btn btn-primary btm-lg" %>
  </div>

<%end%>
